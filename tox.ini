[tox]
envlist = py27,py3

[testenv]
commands = pytest --cov-fail-under=100 -vv
deps = 
    pytest
    pytest-coverage
    pytest-flakes
    pytest-twisted
    klein
    docker
setenv = 
    HOME = {toxinidir}
